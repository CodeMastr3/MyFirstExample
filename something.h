/* This is literally useless but only a place holder */

#include <iostream>

using std::cin;
using std::cout;

void gatherPrint() {
    int temp, temp1;
    cin >> temp >> temp1;
    cout << temp << " " << temp1 << "\n Those are your 2 numbers.\n";
}
